Metadata-Version: 2.4
Name: ashare-quant
Version: 0.1.0
Summary: A-share multi-factor stock selection and portfolio backtesting framework powered by AKShare.
Author: Independent Quant Research
Requires-Python: >=3.10
Description-Content-Type: text/markdown
Requires-Dist: akshare
Requires-Dist: jinja2
Requires-Dist: loguru
Requires-Dist: matplotlib
Requires-Dist: numpy
Requires-Dist: pandas
Requires-Dist: plotly
Requires-Dist: pyarrow
Requires-Dist: pyyaml
Requires-Dist: scikit-learn
Requires-Dist: scipy
Requires-Dist: seaborn
Requires-Dist: statsmodels
Requires-Dist: streamlit
Requires-Dist: typer
Provides-Extra: dev
Requires-Dist: pytest; extra == "dev"
Requires-Dist: ruff; extra == "dev"

# A 股多因子选股与组合回测系统

这是一个基于 Python 与 AKShare 的 A 股 Alpha 研究工程包，覆盖数据读取、复权清洗、因子计算、单因子评价、多因子打分、组合回测、稳健性检验、HTML 报告与 Streamlit 可视化看板。

## 快速开始

```bash
pip install -e ".[dev]"
ashareq quickstart
ashareq run-dashboard
```

默认配置位于 `configs/default.yaml`。示例流程会优先使用 AKShare 获取数据；当网络、限流或接口不可用时，可通过 `--demo` 使用内置模拟数据跑通完整链路。

## CLI 命令

```bash
ashareq fetch-data --config configs/default.yaml
ashareq run-factor --config configs/default.yaml
ashareq run-backtest --config configs/default.yaml
ashareq make-report --config configs/default.yaml
ashareq run-dashboard --config configs/default.yaml
```

## 主要能力

- 数据层：AKShare 行情、指数成分、行业、估值和财务指标适配，本地 Parquet 缓存。
- 清洗层：复权价格、停牌过滤、涨跌停过滤、缺失值、极端值和截面标准化。
- 因子层：动量、反转、波动率、流动性、估值、成长、盈利、杠杆、质量。
- 分析层：分层回测、IC、RankIC、IR、IC 衰减、换手率、行业/市值中性化。
- 组合层：等权、IC 加权、IR 加权、风险平价、多因子综合评分。
- 回测层：调仓频率、持仓上限、手续费、滑点、交易成本、风险指标和滚动窗口。
- 可视化：净值、回撤、月度收益热力图、IC、分层收益、因子相关性、风险暴露。

## 输出目录

- `data/raw/`：AKShare 原始缓存。
- `data/processed/`：清洗后的研究数据。
- `outputs/reports/`：HTML 研究报告。
- `outputs/figures/`：图表文件。
- `outputs/backtests/`：净值、持仓和绩效结果。

## 设计边界

用于研究和项目展示，不构成投资建议！
